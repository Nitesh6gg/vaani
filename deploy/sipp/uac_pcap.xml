<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  Vaani media load test scenario, based on SIPp's stock uac_pcap.xml example.
  Establishes a call, plays a pcap of real RTP audio for the call's duration, and
  hangs up. Combined with `-rtp_echo` on the command line (SIPp echoes back
  whatever RTP it receives), this becomes a self-sustaining closed audio loop for
  the whole call: SIPp -> Asterisk -> Vaani (echo) -> Asterisk -> SIPp -> echoed
  back out -> ... See README.md in this directory for the full invocation.

  a=ptime:20 (below) is a live-diagnosed fix, not decoration: a 10-call PCMA
  run of this scenario without it produced AudioSocket frames Asterisk itself
  declared as 960 bytes (30ms of slin16, not the 640-byte/20ms frame the whole
  pipeline assumes) on every call, discarded as malformed by
  internal/media/audiosocket_call.go, leaving every call totally silent until
  MEDIA_DEAD_TIMEOUT_SECONDS hung it up. A real softphone (MicroSIP) placed the
  same call cleanly with no malformed frames -- the one SDP difference was that
  it declares ptime=20 by default and this scenario never had (Asterisk's own
  `core show channel` confirmed 16kHz was correctly negotiated either way, so
  it's specifically the packetization interval, not the sample rate, that
  was going unstated). Confirm this line actually fixes it before trusting a
  PCMA test run again -- see docs/AUDIO_PIPELINE.md for the write-up once done.
-->
<scenario name="Vaani media load test (UAC with pcap RTP)">
  <send retrans="500">
    <![CDATA[

      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Subject: Vaani load test
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 8 0
      a=rtpmap:8 PCMA/8000
      a=rtpmap:0 PCMU/8000
      a=ptime:20
      a=sendrecv

    ]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>
  <recv response="200" rtd="true"/>

  <send>
    <![CDATA[

      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <!-- Play the pcap once to seed real audio; -rtp_echo keeps the loop alive for
       the rest of the call by echoing back whatever comes in from Asterisk. -->
  <nop>
    <action>
      <exec play_pcap_audio="pcap/g711a.pcap"/>
    </action>
  </nop>

  <!-- Held for the call's duration. This is a plain literal, not
       command-line-configurable (an earlier version of this file claimed `-d`
       on the command line filled in a `[decimal]` placeholder here -- that
       isn't real SIPp behavior; SIPp has no such substitution and just fails
       to parse the literal string as a number). Edit this value directly if
       you want a different hold time; see README.md. -->
  <pause milliseconds="30000"/>

  <send retrans="500">
    <![CDATA[

      BYE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <recv response="200" crlf="true"/>

  <!-- unit is ms -->
  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200"/>
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000, 60000, 65000"/>

</scenario>
